export const environment = {
  production: false,
  API_URL: 'http://localhost:3000/api'
};

/* Para el entorno de producción, se debe crear un archivo environment.prod.ts por separado y definir las variables de entorno correspondientes allí. */